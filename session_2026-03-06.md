# Session Log: 2026-03-06

## Project Context
- **Project:** managed-rag-vertex-v1
- **Tool:** Codex
- **Goal:** Establish session tracking and wait for the next task.

## Starting State
- **Branch:** main
- **Last Working Feature:** Unknown at session start.
- **Known Issues:** None identified yet.

## Session Progress

### [Current Time] - Session initialization
- Confirmed no existing `session_YYYY-MM-DD.md` file was present in the repo root.
- Read repository instructions and local documentation before making changes.
- Created this session log file.
- Files changed: `session_2026-03-06.md`
- Result: Session tracking initialized.

### [Current Time] - Vertex tutorial scaffolding
- Added the state contract for `corpus_name.txt` in `DATA_CONTRACT.md`.
- Added `.env.example` for `VERTEX_PROJECT_ID`, `VERTEX_LOCATION`, and corpus display name.
- Added Vertex SDK dependency to `requirements.txt`; `python-dotenv` was already present.
- Implemented `src/lesson_1_init_vertex.py` and `src/lesson_2_create_corpus.py`.
- Files changed: `DATA_CONTRACT.md`, `.env.example`, `requirements.txt`, `src/lesson_1_init_vertex.py`, `src/lesson_2_create_corpus.py`, `session_2026-03-06.md`
- Result: Lesson 1 and Lesson 2 scaffolding is in place for the Vertex RAG tutorial flow.

### [Current Time] - Runtime SDK correction
- Verified the installed `.venv` Vertex SDK because the web notes did not match the actual API surface.
- Updated Lesson 1 and Lesson 2 to accept existing `GOOGLE_CLOUD_*` settings as fallbacks.
- Rewrote Lesson 2 to use `rag.RagEmbeddingModelConfig`, `rag.VertexPredictionEndpoint`, `rag.RagVectorDbConfig`, and `rag.RagManagedDb`, which match the installed SDK.
- Files changed: `.env.example`, `src/lesson_1_init_vertex.py`, `src/lesson_2_create_corpus.py`, `session_2026-03-06.md`
- Result: Lesson 2 now targets the real runtime API instead of the stale helper syntax.

### [Current Time] - Lesson 2 manual verification
- Confirmed `lesson_1_init_vertex.py` succeeds with the local ADC setup.
- Confirmed the minimal `rag.create_corpus(display_name=...)` flow works in practice.
- Found that `us-east1` returns a live Vertex RAG Engine allowlist/capacity restriction for this project, even though it appears on public docs.
- Successfully created a corpus in `asia-southeast1` and wrote the resource name to `corpus_name.txt`.
- Files changed: `session_2026-03-06.md`
- Result: Lesson 2 is manually verified and the persisted corpus state is now available for later lessons.

### [Current Time] - Phase 2 implementation
- Added `src/lesson_3_import_files.py` to load `corpus_name.txt`, scan `docs/`, and import files with chunking configuration.
- Replaced `src/lesson_4_query.py` with a Vertex RAG generation flow using `Tool.from_retrieval(...)` and `GenerativeModel`.
- Kept the lesson scripts tutorial-oriented and independently runnable.
- Files changed: `src/lesson_3_import_files.py`, `src/lesson_4_query.py`, `session_2026-03-06.md`
- Result: Lesson 3 and Lesson 4 are ready for manual testing against the working corpus.

### [Current Time] - Brief alignment revision
- Read `APP_BRIEF.md` and reconciled it against the working SDK behavior.
- Updated Lesson 1 to print a clean environment summary after Vertex initialization.
- Fixed the Lesson 4 module entrypoint and kept the runtime-correct `Tool.from_retrieval(...)` implementation.
- Files changed: `src/lesson_1_init_vertex.py`, `src/lesson_4_query.py`, `session_2026-03-06.md`
- Result: Lessons 1, 3, and 4 align more closely with the locked brief while preserving runtime-correctness.

### [Current Time] - Uploads directory correction
- Switched Lesson 3 ingestion from `docs/` to the new root `uploads/` directory so test files stay separate from project documentation.
- Updated `APP_BRIEF.md` to reflect `/uploads` as the local ingestion source.
- Files changed: `src/lesson_3_import_files.py`, `APP_BRIEF.md`, `session_2026-03-06.md`
- Result: The lesson flow now matches the actual manual testing setup.

## Lessons Learned
- `AGENTS.md` requires a session file in the repo root before implementation work.
- `python-dotenv` was already pinned in `requirements.txt`, so only the Vertex SDK package needed to be added.
- The installed `vertexai.rag` SDK can differ materially from web snippets, so local runtime inspection is the safer source of truth.
- Public support docs and live service availability can diverge; a region can appear supported while still being restricted for new-project RAG corpus creation.

## End of Session State
- **Working:** Session log exists at the project root. Lessons 1 through 4 now exist in Vertex tutorial form. `DATA_CONTRACT.md` and `.env.example` exist.
- **Broken:** `us-east1` RAG corpus creation is blocked for this project by a live allowlist/capacity restriction.
- **Next Steps:** Manually run Lesson 3 and Lesson 4 against the `asia-southeast1` corpus and verify import plus grounded generation.

## Files Changed This Session
- `session_2026-03-06.md` - Created the initial session log for this session.
- `DATA_CONTRACT.md` - Defined the contract for the persisted Vertex corpus resource name.
- `.env.example` - Added the required Vertex environment variables for the lesson flow.
- `requirements.txt` - Added the Vertex AI SDK dependency.
- `src/lesson_1_init_vertex.py` - Added the ADC and Vertex initialization lesson.
- `src/lesson_2_create_corpus.py` - Added the RagCorpus creation lesson and persistence step.
- `src/lesson_3_import_files.py` - Added the docs-to-corpus import lesson with explicit chunking settings.
- `APP_BRIEF.md` - Updated the ingestion source from `/docs` to `/uploads`.
- `src/lesson_4_query.py` - Replaced the old File Search query lesson with Vertex RAG grounded generation.
