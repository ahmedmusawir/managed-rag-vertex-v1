# Session Log: 2026-03-07

## Project Context
- **Project:** managed-rag-vertex-v1
- **Tool:** Codex
- **Goal:** Continue the Vertex AI RAG Engine tutorial migration and resolve the Lesson 3 ingestion path so the tutorial can proceed from corpus creation to grounded generation.

## Starting State
- **Branch:** main
- **Last Working Feature:** Lesson 1 and Lesson 2 both run successfully. Lesson 2 creates a RagCorpus and persists its resource name to `corpus_name.txt`.
- **Known Issues:** Lesson 3 currently fails because the installed `vertexai.rag` SDK rejects local filesystem paths and requires Google Cloud Storage URIs or Google Drive URLs for `rag.import_files(...)`.

## Session Progress

### [Current Time] - Session rollover
- Reviewed the current repository state after successful manual verification of Lesson 1 and Lesson 2.
- Confirmed the active corpus resource is stored in `corpus_name.txt`.
- Confirmed the test input now lives in the root `uploads/` directory instead of `docs/`.
- Captured the current ingestion blocker for Lesson 3 so the next change can be made against the real SDK behavior.
- Files changed: `session_2026-03-07.md`
- Result: A current session log now exists for today with the project status and active blocker documented.

## Lessons Learned
- Runtime-correct SDK behavior is more reliable than public snippets or overview docs.
- Vertex RAG corpus creation works for this project in `asia-southeast1`, while `us-east1` is blocked by a live allowlist/capacity restriction.
- In the installed SDK path, `rag.import_files(...)` does not accept local files and instead requires `gs://` URIs or Google Drive URLs.

## End of Session State
- **Working:** Lesson 1 initializes Vertex AI and confirms ADC. Lesson 2 creates a corpus successfully. `corpus_name.txt` contains a valid `projects/.../ragCorpora/...` resource path. The root `uploads/` directory is the intended local staging area for test files.
- **Broken:** Lesson 3 still fails when given local file paths because Vertex RAG ingestion requires cloud-hosted sources in this SDK path.
- **Next Steps:** Update Lesson 3 to use a supported ingestion source, most likely by staging files from `uploads/` to GCS and importing the resulting `gs://` paths.

## Files Changed This Session
- `session_2026-03-07.md` - Created today’s session log and documented the current working state plus the Lesson 3 ingestion blocker.
